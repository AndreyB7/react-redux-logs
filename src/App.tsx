import React from 'react';
import LogsList from "./components/LogsList";

const App = () => {
    return (
        <div className='logs'>
            <LogsList/>
        </div>
    );
};

export default App;
